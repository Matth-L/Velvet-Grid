apiVersion: 1

datasources:
  - name: Opensearch
    type: grafana-opensearch-datasource
    url: https://opensearch-node1:9200
    basicAuth: true
    basicAuthUser: "admin"
    editable: true
    jsonData:
      index: "prometheus-metrics"
      tlsSkipVerify: true
      flavor: opensearch
      timeField: "@timestamp"
      version: "3.3.2" # FIX: if it's not working probably here, not generic
    secureJsonData:
      basicAuthPassword: "SecureP@ssword1"
